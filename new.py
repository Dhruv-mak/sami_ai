from openai import OpenAI
from dotenv import load_dotenv
import os
import scanpy as sc
import numpy as np
# load_dotenv()

# adata = sc.read("/home/niklaus/projects/biochem/sami_ai/clustering_052825_224640.h5ad")
# print(f"Regions:{list(adata.obs['region'].unique())}")
# print(f"Clusters:{np.sort([int(item) for item in adata.obs['leiden'].unique()])}")

if True:
    x = 5
    
print(x)